<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.shariful.librarymanagement">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.SEARCH" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable"
                android:value=".SearchableActivity" />
        </activity>
        <activity android:name=".BookCategory" />
        <activity android:name=".CSE_Book_List" />
        <activity android:name=".CIVIL_book_list" />
        <activity android:name=".EEE_book_list" />
        <activity android:name=".AddCseBook" />
        <activity android:name=".AddEeeBook" />
        <activity android:name=".AddCivilBook" />
        <activity android:name=".RegisterStudentList" />
        <activity android:name=".Register_Book"></activity>
    </application>

</manifest>